import 'package:flutter/material.dart';

class AppColors {
  // The soft off-white background color
  static const Color backgroundLight = Color(0xFFE0E5EC);

  // Colors for the Neumorphic shadows
  static const Color shadowLight = Colors.white;
  static const Color shadowDark = Color(0xFFA3B1C6);

  // Text colors
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF888888);

  // Accent colors for the slider and active buttons
  static const Color accent = Colors.black87;
}
